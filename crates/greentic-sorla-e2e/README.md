# greentic-sorla-e2e

Non-publishable end-to-end scenario crate for `greentic-sorla`.

This crate can depend on sibling Greentic workspaces such as
`../greentic-sorla-providers` without affecting the publishable
`greentic-sorla` CLI package.
